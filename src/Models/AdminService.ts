export interface CompanyModel {
    id: number;
    name: string;
    email: string;
    password: string; 
}

export interface CustomerModel {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    password: string; 
}